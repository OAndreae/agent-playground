{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "if [[ \"$file_path\" == *.py ]]; then uv run ruff format \"$file_path\"; fi"
          },
          {
            "type": "command",
            "command": "if [[ \"$file_path\" =~ /api/.*\\.ts$ ]]; then deno fmt \"$file_path\" && deno lint --fix \"$file_path\"; fi"
          }
        ]
      }
    ]
  }
}
